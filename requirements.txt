# Runtime dependencies.
#
# The previous requirements.txt was a full 2019 development-environment freeze.
# Keep this file small so applications can install only what the package imports.
matplotlib>=3.6
nlopt>=2.7
numpy>=1.23
